extends layouts/main.pug

block content
  h2 All members
  -for(i = 0; i < info.length; i++)
    section.persons
      a(href='/' + info[i].id)= info[i].fName + ' ' + info[i].sName
      p= info[i].birthday
      p= info[i].languages
